apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-oci.fullname" . }}
  labels:
    {{- include "data-oci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-oci.selectorLabels" . | nindent 4 }}
